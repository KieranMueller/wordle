server:
  port: 8080
spring:
  config:
    import: env.properties
  mail:
    host: live.smtp.mailtrap.io
    port: 2525
    username: api
    password: c67e7be0a035300a65a828fdfbf76432
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  application:
    name: wordle
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  datasource:
    url: ${prodDBUrl}
    username: ${prodDBUsername}
    password: ${prodDBPassword}
    driver-class-name: com.mysql.cj.jdbc.Driver

